# Targetbot — этап 32: улучшенные карточки и креативные дайджесты

Проект «CI → GitHub → Cloudflare Workers → Telegram-бот отчётов по рекламе Meta» развивается поэтапно. После каркаса,
регистрации чатов, мастера проектов, расписаний, KPI, учёта оплат, Meta OAuth, интерактивных отчётов, автоотчётов с CSV и
мониторинга мы добавили архив отчётов, клиентский портал и довели карточку проекта до полноценной панели управления. На
этапе 22 появился редактор кампаний, на этапе 23 архив получил повторную отправку CSV, на этапе 24 — интерактивный редактор
дайджестов, этап 25 принёс меню «⚡ Массовые действия», этап 26 — readiness-скрипт и `/health` с проверкой секретов, этап 27 —
личные пресеты отчётов, этап 28 — расширенную карточку проекта, этап 29 — выделенные KV namespace, этап 30 — пресеты
дайджестов и быстрый запуск. Этап 31 добавил автоопределение цели кампании и хранение профилей метрик в KV, а этап 32 доводит
интерфейс до готовности: списки проектов показывают чаты и аккаунты со ссылками, карточки подчёркивают статус биллинга, а
дайджесты формируют живые топы и антирейтинги креативов с CSV прямо в личные сообщения администратора.

> **Примечание.** Вся актуальная работа перенесена из ветки `codex/connect-and-deploy-code-to-workers.dev-rqtjth` (ранее
> использовалась как `work`) в основную ветку `main`. Теперь именно `main` содержит свежие исходники, и её следует
> использовать для дальнейшей разработки и автодеплоя.

## Что уже сделано
- Воркер (`src/index.js`) обрабатывает маршруты `/health`, `/`, `/tg`, `/fb_auth`, `/fb_cb`, `/fb_debug`, `/p/{code}`.
- Реализован полноценный OAuth Meta: `fb_auth` проверяет права, `fb_cb` обменивает код на долгоживущий токен, сохраняет профиль и
  сразу пересканирует рекламные аккаунты в KV.
- В `/tg` доступны команды `/start`, `/help`, `/register`, `/admin`, `/report`, `/digest`:
  - `/start` и `/help` возвращают краткую справку.
  - `/register`, вызванный внутри нужного топика, сохраняет `chat_id` и `message_thread_id` в KV (`DB`) и подтверждает привязку.
- `/admin` выводит панель с подключением Meta, разделом «Аккаунты Meta», списком чатов и проектов.
- Раздел «Аккаунты Meta» теперь не только показывает кешированный список аккаунтов, но и подгружает свежие метрики: spend за
  сегодня, лучший и средний CPA, статус биллинга и оценку (🟢/🟡/🔴). Каждая строка кликабельна и ведёт к связанному проекту,
  а при нескольких проектах выводится отдельный список. `/fb_debug` по-прежнему показывает токен, разрешения и счётчики
  `/me/adaccounts` и `/me/businesses`.
- Список зарегистрированных чатов выдаёт кликабельные ссылки на соответствующие топики в Telegram.
- Список проектов отображает не только код и `act_`, но и название чата с гиперссылкой, имя рекламного аккаунта и краткое расписание.
- Карточка проекта показывает актуальный статус рекламного аккаунта из Meta (включая причину блокировки/оплаты), добавлена
  кнопка «💬 Перейти в чат проекта».
- `/report <код> [period]` готовит компактную карточку для клиентского чата и подробный отчёт в ЛС инициатора команды. Цель
  кампании определяется автоматически по `objective`, `optimization_goal`, `promoted_object` и фактическим действиям; профиль
  метрик кэшируется в KV (`profiles:{campaign_id}`), поэтому корректно считаются лиды, диалоги, трафик, вовлечения и охват.
  CSV прикрепляется в чат (по запросу) и в ЛС вместе с расширенной версией отчёта.
- `/digest <код> [period]` формирует дайджест с суммарными метриками, топовыми креативами и проблемными объявлениями. В чат
  отправляется компактная карточка, в ЛС администратора — развёрнутый отчёт с CTR/CPC/CPM, автоматическим антирейтингом
  креативов и CSV по объявлениям. Используется тот же Meta-токен и набор периодов, что и `/report`.
- Кнопка «📤 Отчёт» в карточке проекта открывает меню фильтров: можно выбрать период, задать минимальный расход, переключить
  показ кампаний без результатов и решить, куда отправить отчёт. Предпросмотр появляется в админке (без публикации в чате), а
  отправка применяет те же фильтры к сообщению для клиента.
  На этапе 27 добавлены личные пресеты отчётов: администратор сохраняет текущие фильтры под именем, затем применяет или
  удаляет пресеты прямо из Telegram без повторной настройки.
- Кнопка «📬 Дайджест» в карточке проекта позволяет выбрать период, мгновенно посмотреть итог в админке и отправить дайджест
  в привязанный чат — без команд и ручного ввода. На этапе 30 добавлены личные пресеты дайджестов: администратор сохраняет
  выбранный период под именем, применяет или удаляет пресеты прямо из Telegram.
- Кнопка «⚡ Массовые действия» в карточке проекта открывает меню: можно одним кликом повторно отправить автоотчёт, мгновенно
  сформировать и отправить дайджест по настройкам проекта, запустить еженедельный сводник, вручную прогнать алерты,
  пересчитать автопаузу по KPI и очистить служебные флаги. Каждый запуск пишет короткий отчёт в админский чат и обновляет меню.
- Кнопка «⏸ Поставить кампании на паузу» теперь выводит подтверждение действия; рядом появилась кнопка «▶️ Возобновить
  кампании». Обе команды работают через Meta API и присылают короткий отчёт о результате.
- Редактор «📦 Кампании» дополнился кнопкой «⚙️»: откройте карточку кампании, чтобы увидеть spend/CPA по периодам, список
  адсетов, а также управлять кампанией прямо из Telegram — ставить на паузу, включать, увеличивать бюджеты адсетов на 10%,
  продлевать расписание на 7 дней, переключать отдельные адсеты и управлять объявлениями (pause/resume) с показателями за
  последние 7 дней.
- Планировщик (`scheduled`) каждые 5 минут пробегает проекты: активные и не «paused» получают автоотчёт по расписанию с
  прикреплённым CSV, записью в архив `report:<code>:<ts>` и (при наличии `GS_WEBHOOK`) отправкой данных в Google Sheets; по
  понедельникам отправляется сводник «неделя + сегодня/вчера» согласно настройке weekly. После отчётов запускаются
  автоматические проверки алертов и автопаузы.
- Реализованы алерты: проверка биллинга (проблемные статусы, баланс предоплаты, `spend_cap`), контроль нулевого расхода, а
  также два окна для аномалий (рост CPA, падение CTR/показов, высокая частота) и усталости креативов (частота, CTR, CPA). Все
  уведомления приходят админам с кратким описанием и списком кампаний.
- Автопауза считает streak превышений KPI по CPL (по данным за вчера): хранит состояние в KV, оповещает админов при достижении
  порога и теперь даёт кнопку «⏸ Поставить кампании на паузу» как в карточке проекта, так и прямо в уведомлении — воркер
  вызывает Meta API и присылает отчёт об успешных/неудачных запросах.
- Архив отчётов доступен из карточки проекта: выводится постраничный список записей за последние 90 дней, каждая запись
  показывает период, фильтры, суммарные показатели и источник (`manual`/`auto`/`weekly`). Из архива можно открыть подробный
  предпросмотр, повторно отправить сообщение в чат, получить CSV прямо в админке или переотправить вложение в клиентский чат
  (файл формируется на лету, если не сохранился при исходном отчёте).
- Раздел «🌐 Портал» в карточке проекта позволяет включать и обновлять ссылку, отправлять её клиенту или себе в Telegram.
  Публичный маршрут `/p/{code}?sig=...` рендерит HTML-дашборд с навигацией по периодам, таблицей кампаний, итогами по spend и
  CPA, а также пометками о статусе биллинга и времени обновления.
- Редактор «📦 Кампании» загружает данные из Meta, поддерживает постраничный просмотр, фильтрацию по активным, быстрый выбор
  всех кампаний и мгновенное переключение отдельной кампании с немедленным сохранением в KV.
- Инлайн-кнопки панели обрабатываются без зависаний: чаты и проекты подгружаются из KV с пагинацией, остальные действия пока
  сообщают о незавершённой реализации.
- Каркас данных проекта (`project:<code>`) и временных состояний (`state:<uid>`) нормализуется вспомогательными функциями.
- Добавлен мастер создания проекта: в админ-панели появилась кнопка «➕ Новый проект». Мастер по шагам просит указать код,
  выбрать чат/топик из зарегистрированных, ввести `act_` рекламного аккаунта, затем выбрать период отчётов и стартовое
  расписание (пресеты либо ручной ввод). После завершения проект сохраняется в KV и отображается в списке «Проекты».
- После выбора расписания мастер предлагает настроить биллинг: выбрать состояние «активен»/«пауза», отметить оплату «сегодня»,
  ввести дату вручную (с автоматическим расчётом следующего платежа) или очистить значения.
- Следующий шаг мастера — первичная настройка KPI: кнопками задаются CPL, лиды/день и бюджет/день, есть быстрый сброс и подсказки
  для ручного ввода значений прямо в Telegram.
- Шаг настройки алертов позволяет включить или отключить уведомления, выбрать пресет окон (`10:00`, `10/14/18`) и управлять
  zero-spend контролем до сохранения проекта.
- Финальный шаг мастера даёт сразу включить/выключить автоотчёты, определить режим еженедельного сводника («неделя+сегодня»
  или «неделя+вчера») и настроить автопаузу: доступны пресеты 2/3/5/7/10 дней, ручной ввод и мгновенное отключение.
- Мастер защищён от распространённых ошибок: проверяется формат кода, дубли, отсутствие зарегистрированных чатов, корректность
  рекламного аккаунта и выбор периода. Любой шаг можно отменить кнопкой «Отмена». При ручном вводе расписания проверяется
  формат времени.
- Список проектов получил кнопки «⚙️ код», открывающие карточку выбранного проекта. Карточка показывает аккаунт, чат,
  расписание, KPI, статусы сводника, автопаузы и алертов, а также даты оплат.
- Из карточки можно управлять состоянием проекта: включать/выключать автоотчёты, ставить биллинг на паузу, переключать сводник,
  автопаузу по KPI, запускать редакторы расписания, KPI и алертов.
- Редактор расписания поддерживает добавление и удаление времён, быстрые кнопки популярных слотов, ввод произвольного времени,
  переключение периодов отчёта и тихих выходных. Все изменения сохраняются в KV и сразу отражаются в интерфейсе.
- Редактор KPI открывается из карточки проекта: можно задать CPL, целевые лиды в день и дневной бюджет, очистить показатели или
  отменить ввод. После сохранения значения сразу отображаются в карточке.
- Добавлен редактор автопаузы: задавайте порог (пресеты 2/3/5/7/10 дней, произвольное значение, сброс по умолчанию), отключайте
  режим или меняйте его прямо из Telegram. При вводе «нет» автопауза выключается.
- Реализован учёт оплат: в карточке появился раздел «💵 Оплата» с быстрым занесением платежа «сегодня», ручным вводом дат,
  расчётом следующего платежа (+1 месяц) и очисткой данных. При вводе дат принимаются форматы `YYYY-MM-DD`, `ДД.ММ.ГГГГ` и
  ключевые слова «сегодня»/«вчера». Мастер проекта умеет задавать эти параметры ещё до сохранения проекта.
- Редактор алертов позволяет включать/выключать уведомления, управлять окнами billing-проверок (пресеты и произвольные значения)
  и задавать время zero-spend либо отключать контроль. Все изменения сразу отражаются в карточке.
- При создании проекта можно сразу выбрать период, расписание, биллинг, KPI, алерты, автоотчёты, weekly-сводник и автопаузу;
  итоговое сообщение показывает ключевые настройки и напоминает о дальнейших шагах.
- Вызовы Telegram API обёрнуты в `fetchWithTimeout`, чтобы воркер не зависал при сетевых проблемах.
- Код использует раздельные namespace (`REPORTS_NAMESPACE`, `BILLING_NAMESPACE`, `LOGS_NAMESPACE`): они уже прописаны в
  `wrangler.toml` и указывают на боевой KV (`c55f82210ade457c93aff4fe2ba8b007`). При необходимости можно добавить preview ID,
  fallback на `DB` сохранён для совместимости. Крон каждые 5 минут и расширенные настройки Observability остаются активны.
- Скрипт `npm run check:config` проверяет обязательные секреты и KV binding в `wrangler.toml`. Он сначала читает локальные
  `.dev.vars`/`.env`, а затем (если доступно) запускает `npx wrangler secret list --json`, чтобы убедиться, что переменные уже
  сохранены в Cloudflare. При необходимости можно запустить `npm run check:config -- --require-dedicated-kv`, чтобы убедиться в
  наличии отдельных namespace, а флаг `--ping-telegram` дополнительно выполнит запрос `getMe` к Bot API и подтвердит токен.
  Эндпоинт `/health` теперь возвращает 503 и список пропущенных переменных при неполной конфигурации.
- Сценарии npm (`npm run dev`, `npm run deploy`) используют `npx wrangler@4.46.0`, поэтому дополнительных зависимостей
  устанавливать не требуется. GitHub Actions workflow выполняет автодеплой из ветки `main`.

## Приоритетный бэклог (сверху — критично)
1. **Настроить окружение и секреты.** Заполнить `BOT_TOKEN` (поддерживаются алиасы `TG_API_TOKEN`, `TG_BOT_TOKEN`,
   `TELEGRAM_BOT_TOKEN`, `TELEGRAM_TOKEN`, `TELEGRAM_API_TOKEN`, `TELEGRAM_BOT_API_TOKEN`), `ADMIN_IDS`, `FB_APP_ID`,
   `FB_APP_SECRET`, `DEFAULT_TZ`, `WORKER_URL`, при необходимости `FB_LONG_TOKEN` и `GS_WEBHOOK`; добавить автоматическую
   проверку перед деплоем. Если переменная `ADMIN_IDS` пуста или содержит неверный формат, воркер всё равно добавит
   администраторов по умолчанию — `7623982602` и `573424022`.
   * Скрипт `npm run check:config` теперь подсвечивает, какие переменные с токеном найдены локально или в Cloudflare Secrets:
     при флаге `--ping-telegram` выполняется `getMe`, а `--check-webhook` вызывает `getWebhookInfo`, сверяет URL с `WORKER_URL`,
     предупреждает о накопленных обновлениях и выводит последнюю ошибку вебхука.
   * Эндпоинт `/health` в воркере показывает, из какой переменной взят токен и какие алиасы заданы в окружении. Опция
     `/health?ping=telegram` (или `?ping=all`) пингует Bot API, добавляет latency и username бота и теперь возвращает блок
     webhook: фактический URL, совпадение с ожидаемым, счётчик необработанных обновлений и последние ошибки синхронизации.
2. **Массовые сценарии и пресеты.** Добавить общие (командные) пресеты для отчётов/дайджестов, пакетную отправку дайджестов по списку
   проектов и быстрый перезапуск всех алертов из карточки проекта.
3. **Статистика и mute для алертов.** Хранить историю срабатываний, показывать её в карточке проекта, добавить временные выключатели и
   уровень серьёзности уведомлений.
4. **Observability и мониторинг.** Настроить дашборды Cloudflare Logs/Traces или внешнюю агрегацию, собирать метрики cron-задач.
5. **Автоматические проверки.** Добавить smoke-тесты или прогон Wrangler preview, чтобы гарантировать работоспособность перед деплоем.
6. **Эксплуатационная документация.** Подготовить подробное руководство для операторов: сценарии массовых действий, чек-листы, FAQ.

## Быстрый старт
```bash
# проверка конфигурации и обязательных секретов
npm run check:config
# с пингом Telegram Bot API
npm run check:config -- --ping-telegram
# проверка webhook и сравнение с WORKER_URL
npm run check:config -- --check-webhook

# локальный предпросмотр (нужны ENV и preview KV ID)
npm run dev

# после настройки секретов деплой вызывается той же командой
npm run deploy
```

> Если на локальной машине не настроен `wrangler login`, установите переменную
> `CHECK_CONFIG_SKIP_WRANGLER=1`, чтобы пропустить удалённую проверку секретов.
>
> Если npm сообщает предупреждение `Unknown env config "http-proxy"`, очистите локальные настройки:
> ```bash
> npm config delete http-proxy
> npm config delete https-proxy
> unset npm_config_http_proxy npm_config_https_proxy HTTP_PROXY HTTPS_PROXY
> ```

Продолжайте развивать функциональность по шагам, проверяя каждое изменение локально и через деплой.
