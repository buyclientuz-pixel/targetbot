# TargetBot — Cloudflare Worker + Telegram + Meta Admin Portal

TargetBot — это серверless-проект на Cloudflare Workers, объединяющий Telegram-бота, REST API, админ-панель и интеграцию с Facebook (Meta) Ads.

## 📁 Структура проекта

```
src/
├── index.ts             # точка входа воркера
├── core/                # роутер, утилиты, доступ к KV/R2
├── bot/                 # логика Telegram-бота и webhooks
├── api/                 # REST API (leads, users, reports, settings)
├── meta/                # OAuth и интеграция с Graph API
├── admin/               # HTML + JS админ-панели (Tailwind CDN)
└── tests/               # примеры тестов (опционально, требуют Vitest)
```

## 🚀 Возможности

- `/telegram/:botId` — webhook Telegram, команды `/start`, `/lead`, `/report`, `/status`.
- `/admin?key=!Lyas123` — веб-панель с дашбордом, лидами, пользователями и интеграциями.
- `/api/*` — REST API (CRUD лидов, управление пользователями включая удаление, отчёты, настройки).
- `/api/leads/:id` (DELETE) — удаление заявок через REST API и из админ-панели.
- `/api/dashboard` — агрегированные метрики для дашборда админ-портала.
- `/auth/facebook` и `/auth/facebook/callback` — OAuth Meta.
- `/meta/sync`, `/meta/stats`, `/meta/refresh` — операции с Facebook Ads.
- `/meta/status` — состояние подключения Meta, последний синк и сведения о токене.
- KV-хранилище для пользователей, лидов, токенов и логов; R2 для отчётов.
- Ручной деплой через `npm install` и `npx wrangler deploy`.


## 🛠️ Подготовка окружения

1. Установите Node.js 22 и npm 10.9.2.
2. Проверьте файл `.env` — репозиторий уже содержит базовую конфигурацию с рабочими значениями. При необходимости скопируйте его в `.dev.vars` для локального `wrangler dev` и обновите токены под свою инфраструктуру.
3. Убедитесь, что KV Namespaces и R2 bucket настроены в Cloudflare.

## 📦 Установка зависимостей

Перед локальной работой установите зависимости стандартной командой npm:

```bash
npm install
```

Если команда недоступна в изолированной среде, выполните установку локально и повторите шаги деплоя.

## 🔧 Подготовка админ-панели

HTML админки собирается скриптом `scripts/embed-admin.mjs`, который встраивает стили и JavaScript в страницу воркера. Запустите его вручную после правок UI:

```bash
node scripts/embed-admin.mjs
```

## 🚀 Manual Deployment (Ручной деплой)

Автоматический CI/CD отключён, поэтому развёртывание выполняется вручную. Последовательность действий следующая:

1. Установите зависимости:

   ```bash
   npm install
   ```

2. Соберите TypeScript-исходники в соответствии с `tsconfig.json`:

   ```bash
   npm run build
   ```

3. Выполните деплой на Cloudflare Workers:

   ```bash
   npx wrangler deploy
   ```

✅ После выполнения этих шагов воркер будет обновлён вручную без участия GitHub Actions.

> Для подробного чек-листа локальной проверки см. [Cloudflare Manual Verification Guide](docs/cloudflare-manual-check.md), который описывает запуск `wrangler dev`, тестирование ключевых эндпоинтов и подтверждение успешного деплоя.

## 🧪 Опциональные тесты

Каталог `src/tests/` содержит примеры модульных проверок на [Vitest](https://vitest.dev/). По умолчанию зависимость на Vitest
не входит в `package.json`, чтобы не тянуть лишние пакеты при ручном деплое. Если хотите задействовать тесты локально:

1. Установите dev-зависимости: `npm install --save-dev vitest @vitest/ui @vitest/coverage-v8`.
2. Уберите `src/tests` из исключений `tsconfig.json`, чтобы TypeScript снова анализировал тестовые файлы.
3. Запустите проверки командой `npx vitest run`.

При ручном деплое эти шаги не требуются — тесты служат справочными примерами и не участвуют в сборке воркера.

## 🔐 Переменные окружения

| Переменная | Назначение |
| ---------- | ---------- |
| `DEFAULT_TZ` | Часовой пояс для cron/отчётов |
| `ADMIN_IDS` | Список Telegram ID админов через запятую |
| `BOT_TOKEN` / `TELEGRAM_BOT_TOKEN` | Токен Telegram-бота для API и Webhook |
| `WORKER_URL` | Публичный URL Cloudflare Worker |
| `ADMIN_ID` / `ADMIN_KEY` | Основной админ и ключ доступа к порталу |
| `TELEGRAM_SECRET` | Секрет для подписи внутренних хук-обработчиков |
| `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_AC_ID` | Токен и аккаунт Cloudflare для ручного деплоя |
| `FB_APP_ID` / `FB_APP_SECRET` / `FB_LONG_TOKEN` | Настройки Facebook приложения и длинный токен |
| `R2_ACCESS_KEY_ID` / `R2_SECRET_ACCESS_KEY` | Доступы к R2 бакету |
| `R2_BUCKET_NAME` / `R2_ACCOUNT_ID` / `R2_ENDPOINT` | Идентификаторы и endpoint бакета R2 |

## 🔑 API-ключи и подпись запросов

- Все обращения к REST API и служебным эндпоинтам должны содержать заголовок `X-Auth-Key` или `Authorization: Bearer <key>`.
- Панель `/admin` позволяет создавать и отзывать ключи партнёров, менеджеров и сервисов. Ключи хранятся в `KV_META`.
- При каждом использовании ключа фиксируется время последнего обращения — это видно в разделе «Настройки → API ключи».

> Совет: для начальной настройки используйте `ADMIN_KEY` в качестве `X-Auth-Key`, а затем сгенерируйте индивидуальные ключи для каждой интеграции.

## 📡 Webhook Telegram

Запрос `GET /manage/telegram/webhook?action=status&key=!Lyas123` возвращает состояние вебхука. Доступны действия `drop` и `refresh` (с параметром `url=`).

## 📊 Метрики и дашборд

- `/meta/sync?ad_account_id=act_<ID>` — синхронизация кампаний и кеширование списка в `KV_META`.
- `/meta/stats?ad_account_id=act_<ID>&campaign_id=<ID>` — сбор CPL/CTR с сохранением агрегатов в `meta:stats:last`.
- `/api/dashboard` — выдаёт единую сводку (лиды сегодня/вчера, статистика Meta, статус Telegram webhook).
- `/meta/status` — возвращает данные о подключении (аккаунт, кампания, срок действия токена, последний синк).

## ⚙️ Управление Meta из админки

- Кнопка «Синхронизировать Meta» запрашивает `ad_account_id` и (опционально) `campaign_id`, передаёт их в `/meta/sync` и после обновляет статистику.
- Вкладка «Интеграции» показывает текущий статус подключения, дату последнего обновления токена и итоговые показатели Meta.
- После ручного обновления вебхука админка автоматически перечитывает статус, чтобы отобразить актуальную информацию.

## 🤖 Разработка Telegram-бота

Команда `/lead Имя | Контакт | Комментарий` создаёт заявку в KV и отображает её в админке. `/report` и `/status` возвращают список отчётов и статус Meta интеграции.

## 🔎 Фильтрация лидов

Эндпоинт `GET /api/leads` поддерживает фильтрацию без дополнительной пагинации:

| Параметр       | Значение                                         |
| -------------- | ------------------------------------------------ |
| `status`       | `new`, `in_progress`, `closed` или `all`         |
| `source`       | Произвольное текстовое значение или `all`        |
| `from` / `to`  | Дата в формате `YYYY-MM-DD` (границы включительно) |

Фильтры доступны в админке на вкладке **Leads** и применяются сразу при выборе значения. В ответе API возвращаются также списки доступных статусов и источников, чтобы фронтенд мог построить выпадающие списки без жёстко прописанных значений.

- Для удаления лишних записей используйте кнопку «Удалить» в таблице лидов или вызовите `DELETE /api/leads/:id` с заголовком `X-Auth-Key`.

## 📄 Лицензия

Проект распространяется в рамках частной разработки TargetBot.
