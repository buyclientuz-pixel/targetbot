# TargetBot — Cloudflare Worker + Telegram + Meta Admin Portal

TargetBot — это серверless-проект на Cloudflare Workers, объединяющий Telegram-бота, REST API, админ-панель и интеграцию с Facebook (Meta) Ads.

## 📁 Структура проекта

```
src/
├── index.ts             # точка входа воркера
├── core/                # роутер, утилиты, доступ к KV/R2
├── bot/                 # логика Telegram-бота и webhooks
├── api/                 # REST API (leads, users, reports, settings)
├── meta/                # OAuth и интеграция с Graph API
├── admin/               # HTML + JS админ-панели (Tailwind CDN)
└── tests/               # vitest-тесты
```

## 🚀 Возможности

- `/telegram/:botId` — webhook Telegram, команды `/start`, `/lead`, `/report`, `/status`.
- `/admin?key=!Lyas123` — веб-панель с дашбордом, лидами, пользователями и интеграциями.
- `/api/*` — REST API (CRUD лидов, пользователей, отчётов, настройки).
- `/api/dashboard` — агрегированные метрики для дашборда админ-портала.
- `/auth/facebook` и `/auth/facebook/callback` — OAuth Meta.
- `/meta/sync`, `/meta/stats`, `/meta/refresh` — операции с Facebook Ads.
- KV-хранилище для пользователей, лидов, токенов и логов; R2 для отчётов.
- Ручной деплой через `npm install` и `npx wrangler deploy`.


## 🛠️ Подготовка окружения

1. Установите Node.js 22 и npm 10.9.2.
2. Скопируйте `.env.example` → `.env` и `.dev.vars.example` → `.dev.vars`, после чего заполните реальные секреты.
3. Убедитесь, что KV Namespaces и R2 bucket настроены в Cloudflare.

### 🔒 Почему в репозитории нет «боевого» `.env`

Ценности безопасности и соответствия политике Cloudflare не позволяют хранить в Git реальный файл `.env` с рабочими токенами.
Репозиторий содержит только шаблон с заглушками — это осознанное решение, предотвращающее случайную утечку ваших данных через
публичные fork'и, архивацию бэкапов или автоматические отчёты. Чтобы восстановить прежнюю конфигурацию:

1. Найдите локальную резервную копию `.env`, которая использовалась до очистки.
2. Либо запросите свежие секреты у владельцев интеграций (Telegram, Meta, Cloudflare) и заполните их заново на основе `.env.example`.
3. При необходимости задокументируйте значения в защищённом хранилище (например, 1Password, Bitwarden, Vault).

> Мы принципиально не коммитим реальные токены, даже по запросу, чтобы не нарушать правила безопасности и не привести к блокировке
> аккаунтов. Если `.env` был случайно удалён на вашей машине, восстановить его можно только из ваших собственных бэкапов или через
> повторный выпуск секретов у поставщиков услуг.

## 📦 Установка зависимостей

```bash
npm install
```

> ⚠️ В офлайн-окружении установка пакетов недоступна. В таком случае выполните установку локально перед деплоем.

## 🔍 Сборка и тесты

```bash
npm run test       # запуск vitest
npm run typecheck  # проверка типов
npm run build      # dry-run деплоя через wrangler
npm run bundle:admin  # вручную пересобрать HTML админки в page.ts (обычно запускается автоматически)
```

## 🚀 Ручной деплой

Автоматизированный конвейер GitHub Actions отключён — используйте ручной деплой.

После установки зависимостей запустите развёртывание вручную:

```bash
npm install
npx wrangler deploy
```

## 🔐 Переменные окружения

| Переменная | Назначение |
| ---------- | ---------- |
| `TELEGRAM_TOKEN` | Токен Telegram-бота |
| `FACEBOOK_APP_ID` / `FACEBOOK_APP_SECRET` | Настройки Facebook приложения |
| `FACEBOOK_REDIRECT_URL` | Redirect URL для OAuth |
| `WORKER_PUBLIC_URL` | Публичный URL воркера |
| `ADMIN_KEY` | Ключ доступа к админке |
| `JWT_SECRET` | Секрет для подписей (будущие расширения) |
| `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` | Доступы для `wrangler deploy` |

## 🔑 API-ключи и подпись запросов

- Все обращения к REST API и служебным эндпоинтам должны содержать заголовок `X-Auth-Key` или `Authorization: Bearer <key>`.
- Панель `/admin` позволяет создавать и отзывать ключи партнёров, менеджеров и сервисов. Ключи хранятся в `KV_META`.
- При каждом использовании ключа фиксируется время последнего обращения — это видно в разделе «Настройки → API ключи».

> Совет: для начальной настройки используйте `ADMIN_KEY` в качестве `X-Auth-Key`, а затем сгенерируйте индивидуальные ключи для каждой интеграции.

## 📡 Webhook Telegram

Запрос `GET /manage/telegram/webhook?action=status&key=!Lyas123` возвращает состояние вебхука. Доступны действия `drop` и `refresh` (с параметром `url=`).

## 📊 Метрики и дашборд

- `/meta/sync?ad_account_id=act_<ID>` — синхронизация кампаний и кеширование списка в `KV_META`.
- `/meta/stats?ad_account_id=act_<ID>&campaign_id=<ID>` — сбор CPL/CTR с сохранением агрегатов в `meta:stats:last`.
- `/api/dashboard` — выдаёт единую сводку (лиды сегодня/вчера, статистика Meta, статус Telegram webhook).

## 🤖 Разработка Telegram-бота

Команда `/lead Имя | Контакт | Комментарий` создаёт заявку в KV и отображает её в админке. `/report` и `/status` возвращают список отчётов и статус Meta интеграции.

## 🔎 Фильтрация лидов

Эндпоинт `GET /api/leads` поддерживает фильтрацию без дополнительной пагинации:

| Параметр       | Значение                                         |
| -------------- | ------------------------------------------------ |
| `status`       | `new`, `in_progress`, `closed` или `all`         |
| `source`       | Произвольное текстовое значение или `all`        |
| `from` / `to`  | Дата в формате `YYYY-MM-DD` (границы включительно) |

Фильтры доступны в админке на вкладке **Leads** и применяются сразу при выборе значения. В ответе API возвращаются также списки доступных статусов и источников, чтобы фронтенд мог построить выпадающие списки без жёстко прописанных значений.

## 📄 Лицензия

Проект распространяется в рамках частной разработки TargetBot.
