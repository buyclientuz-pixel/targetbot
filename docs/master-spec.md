# TargetBot — Полное ТЗ, тексты и мастер-промпт

Этот документ объединяет "жирное" ТЗ, шаблоны сообщений и мастер-промпт, который можно передать Codex'у/ИИ-разработчику для доведения проекта до полностью рабочего состояния. Опираться нужно на существующую инфраструктуру Cloudflare Worker + Telegram Bot + Web Portal и уже настроенные переменные окружения.

## 0. Общие условия

* **Никаких новых переменных окружения**. Использовать только уже присутствующие в `.env`/Wrangler:
  - `DEFAULT_TZ`, `ADMIN_IDS`, `ADMIN_ID`
  - `BOT_TOKEN`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_SECRET`
  - `WORKER_URL` (`th-reports.buyclientuz.workers.dev`)
  - `CLOUD_FLARE_API_TOKEN`, `CLOUD_FLARE_AC_ID`
  - `FB_APP_ID`, `FB_APP_SECRET`, `FB_LONG_TOKEN`
  - `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_ACCOUNT_ID`, `R2_ENDPOINT`

* **Webhook Telegram**: `https://${WORKER_URL}/tg-webhook?secret=${TELEGRAM_SECRET}`.
* **Портал проекта**: `https://${WORKER_URL}/p/{projectId}?token=...`.

## 1. Архитектура хранения данных

### 1.1. Workers KV — быстрые данные

Использовать существующие namespaces и ровно такие ключи/форматы:

* `facebook-auth:{userId}` (и зеркальный алиас `fb_auth:{userId}` для легаси-ключей)

```json
{
  "user_id": 7623982602,
  "access_token": "EAAG...",
  "expires_at": "2026-01-13T08:23:00Z",
  "ad_accounts": [
    {
      "id": "act_813372877848886",
      "name": "birlash",
      "currency": "USD"
    }
  ]
}
```

* `projects_by_user:{userId}`

```json
{
  "projects": ["proj_birlash_1", "proj_client_2"]
}
```

* `project:{projectId}`

```json
{
  "id": "proj_birlash_1",
  "name": "birlash",
  "owner_id": 7623982602,
  "ad_account_id": "act_813372877848886",
  "chat_id": -1003269756488,
  "portal_url": "https://th-reports.buyclientuz.workers.dev/p/proj_birlash_1",
  "settings": {
    "currency": "USD",
    "timezone": "Asia/Tashkent",
    "kpi": {
      "mode": "auto",
      "type": "LEAD",
      "label": "Лиды"
    }
  }
}
```

* `billing:{projectId}`

```json
{
  "tariff": 500,
  "currency": "USD",
  "next_payment_date": "2025-12-15",
  "autobilling": true
}
```

* `alerts:{projectId}`

```json
{
  "enabled": true,
  "channel": "chat",
  "types": {
    "lead_in_queue": true,
    "pause_24h": true,
    "payment_reminder": true
  },
  "lead_queue_threshold_hours": 1,
  "pause_threshold_hours": 24,
  "payment_reminder_days": [7, 1]
}
```

* `autoreports:{projectId}`

```json
{
  "enabled": true,
  "time": "10:00",
  "mode": "yesterday_plus_week",
  "send_to": "both"
}
```

### 1.2. R2 — тяжёлые данные

Всё хранить в существующем бакете `R2_BUCKET_NAME`:

* `project-leads/{projectId}/list.json`

```json
{
  "stats": {
    "total": 168,
    "today": 2
  },
  "leads": [
    {
      "id": "lead_1",
      "name": "Farhod",
      "phone": "+998...",
      "created_at": "2025-11-14T16:35:00Z",
      "source": "Facebook",
      "campaign_name": "BirLash Лиды",
      "status": "new"
    }
  ]
}
```

* `project-leads/{projectId}/{leadId}.json` — полная карточка лида.
* `meta/campaigns/{projectId}.json`

```json
{
  "period": {
    "from": "2025-11-15",
    "to": "2025-11-15"
  },
  "summary": {
    "spend": 17.28,
    "impressions": 6500,
    "clicks": 120,
    "leads": 5,
    "messages": 8
  },
  "campaigns": [
    {
      "id": "123",
      "name": "BirLash Лиды",
      "objective": "LEAD_GENERATION",
      "kpi_type": "LEAD",
      "spend": 10.5,
      "impressions": 3200,
      "clicks": 60,
      "leads": 3,
      "messages": 0
    }
  ]
}
```

* `payments/{projectId}/history.json`

```json
{
  "payments": [
    {
      "id": "pay_1",
      "amount": 500,
      "currency": "USD",
      "period_from": "2025-11-15",
      "period_to": "2025-12-15",
      "paid_at": "2025-11-15T18:11:00Z",
      "status": "paid",
      "comment": "Оплата за месяц"
    }
  ]
}
```

## 2. Telegram-бот — тексты и действия

### 2.1. Главное меню `/start`

Сообщение:

```
Главное меню
Выберите раздел: авторизация, проекты, аналитика или финансовые настройки.
```

Reply-клавиатура:
1. Авторизация Facebook
2. Проекты
3. Аналитика
4. Пользователи
5. Финансы
6. Вебхуки Telegram
7. Настройки

### 2.2. Авторизация Facebook

**Если токена нет** — показать текст со ссылкой и инструкциями, открыть OAuth-страницу и при необходимости принять токен сообщением, записать в `facebook-auth:{userId}` (плюс `fb_auth:{userId}` для совместимости) и подтвердить подключение.

**Если токен есть** — вывести статус, дату истечения, кнопки `🔄 Обновить токен`, `📦 Список рекламных аккаунтов`, `⬅️ Назад`.

Кнопка «Авторизация Facebook» в главном меню открывает `https://${WORKER_URL}/api/meta/oauth/start?tid={telegramUserId}` — прямой OAuth-flow с редиректом на `https://www.facebook.com/v18.0/dialog/oauth`. HTML-страница `/fb-auth` остаётся как подсказка, но не основной вход.

### 2.3. Проекты

* Читать `projects_by_user:{userId}`.
* Если пусто — сообщить, что проектов нет.
* Иначе — список вида `1️⃣ birlash [14,78 $]` (затраты за сегодня из summary.spend), кнопки: каждый проект, `➕ Новый проект`, `⬅️ Назад`.

### 2.4. Карточка проекта

Сообщение с полями Meta, CPA, лиды, оплата, автобиллинг, автоотчёты, алерты, чат, портал. CPA = spend_today / KPI (например leads). Формат текста строго как в ТЗ.

Inline-кнопки:
`✏️ Изменить данные`, `📲 Чат-группа`, `🔁 Изменить чат-группу`, `🚫 Отвязать чат`, `💬 Лиды`, `📈 Отчёт по рекламе`, `👀 Рекламные кампании`, `📤 Экспорт данных`, `🧩 Портал`, `💳 Оплата`, `🕒 Авто-отчёты`, `⚙ Изменить KPI проекта`, `🧨 Удалить`, `⬅️ К списку`, `🏠 Меню`.

## 3. Логика по кнопкам

### 3.1. `✏️ Изменить данные`

Показать меню выбора поля (название, рекламный кабинет, владелец). После выбора ожидать новое значение, обновлять `project:{projectId}`.

### 3.2. `📲 Чат-группа`

Если `chat_id` есть — показать ссылку, ID и кнопки `🔁 Изменить`, `🚫 Отвязать`, `⬅️ Назад`. Если нет — предложить привязать чат.

### 3.3. `🔁 Изменить чат-группу`

Сообщение с инструкциями (выбрать из списка свободных чатов или отправить ссылку/ID). Inline-кнопки: список чатов, `🔗 Отправить ссылку на чат вручную`, `⬅️ Назад к проекту`. После выбора сохранить `chat_id` и подтвердить.

### 3.4. `🚫 Отвязать чат`

Подтверждение с кнопками `✅ Да, отвязать`, `⬅️ Отмена`. При подтверждении — `chat_id = null`, сообщение об успешном отвязывании.

### 3.5. `💬 Лиды`

Заголовок с total/today. Кнопки фильтров статусов, `📤 Экспорт лидов`, `⬅️ Назад`. Для каждого фильтра показывать последние 5–10 карточек с именем, телефоном, датой, кампанией, временем в очереди. Кнопки карточки: `✅ Завершить`, `⏳ В обработке`, `🗑 В корзину`, `📞 Позвонен / Написан`, `⬅️ Назад к списку`.

### 3.6. `📈 Отчёт по рекламе`

Использовать `meta/campaigns/{projectId}.json`. Текст:

```
Отчёт по рекламе — {project.name}
Период: вчера + неделя

💰 Затраты: {spend} {currency}
👀 Показов: {impressions}
👆 Кликов: {clicks}
🎯 KPI: {kpi_label} — {kpi_value}
📊 CPA: {cpa} {currency}

Топ-3 кампании по KPI:
1️⃣ ...
```

Кнопки: `📅 Изменить период`, `⚙ Настройки KPI`, `⬅️ Назад`.

### 3.7. `📤 Экспорт данных`

Кнопки: `💬 Лиды (CSV)`, `📈 Кампании (CSV)`, `💳 Оплаты (CSV)`, `⬅️ Назад`. Ответ — файл + текст «Вот ваш экспорт в формате CSV».

### 3.8. `💳 Оплата`

Читать `billing:{projectId}` и `payments/{projectId}/history.json`. Сообщение с тарифом, датой платежа, статусом автобиллинга, история (если есть). Кнопки: `📅 +30 дней`, `350$`, `500$`, `📅 Указать дату оплаты`, `✏️ Ввести дату вручную`, `⬅️ Назад`. При ожидании даты любое сообщение парсится (форматы `DD.MM.YYYY` или `YYYY-MM-DD`). Обновить KV и историю оплат. Подтверждения вида «✅ Дата следующего платежа обновлена: 15.12.2025».

### 3.9. `🕒 Авто-отчёты`

Сообщение с текущими настройками из `autoreports:{projectId}`. Кнопки: `✅ Включить / ⛔️ Выключить`, `🕒 Изменить время`, `👥 Кому отправлять`, `⬅️ Назад`.

### 3.10. `⚙ Изменить KPI проекта`

Сообщение с режимом и типом KPI. Кнопки: `🤖 Авто-режим`, `📝 Ручной выбор`, `🎯 Лиды`, `💬 Сообщения`, `👆 Клики`, `👀 Просмотры`, `🛒 Покупки`, `⬅️ Назад`.

### 3.11. `🧨 Удалить`

Подтверждение удаления с кнопками `🧨 Да, удалить проект`, `⬅️ Отмена`. При подтверждении удалить записи KV/R2 и отправить «✅ Проект удалён».

## 4. Алерты и автоотчёты

### 4.1. Напоминание по лидам

```
🔔 Лид ожидает ответа
Имя: {name}
Телефон: {phone}
Получен: {dd.mm.yyyy, HH:MM:SS}
В ожидании: {hh ч mm мин}
Проект: {project.name}

Откройте список лидов в боте, чтобы обновить статус.
```

### 4.2. Кампании на паузе >24ч

```
⚠️ Кампании на паузе >24ч — {project.name}

1️⃣ {campaign_name} — на паузе с {дата/время}
...

Проверьте кампании в рекламном кабинете.
```

### 4.3. Напоминание об оплате

```
📄 Напоминание об оплате
Оплата запланирована на {next_payment_date} по проекту {project.name}.
Тариф: {tariff} {currency}.
```

## 5. Клиентский портал

* При загрузке `/p/{projectId}?token=...` показывать модальный прелоадер «Готовим данные… Это может занять 3–5 секунд».
* JS запрашивает параллельно:
  - `/api/project/{projectId}?token=...`
  - `/api/billing/{projectId}?token=...`
  - `/api/campaigns/{projectId}?token=...`
  - `/api/leads/{projectId}?token=...`
* Таймаут 10–15 секунд: при ошибке скрыть прелоадер и показать «Не удалось загрузить данные. [Повторить попытку]».
* Верхний блок «Ключевые показатели»: расход, показы, клики, KPI, лиды всего, новые лиды.
* Таблица лидов: Имя, Телефон, Тип, Дата, Реклама.
* Таблица кампаний: Название, Статус, Цель, Показатель, Расход, Показов, Клики.
* Блок оплаты: текущий тариф (`billing`), дата следующего платежа, история оплат (`payments/.../history.json`).

## 6. Таблица «Команда → данные → события»

Примеры (необходимо продолжить по всем командам):

```
КОМАНДА: "Проекты"
ДАННЫЕ:
  - kv.get("projects_by_user:{userId}")
  - для каждого projectId: kv.get("project:{projectId}")
  - R2.get("meta/campaigns/{projectId}.json") для получения spend_today
СОБЫТИЕ:
  - показать список проектов с текущими затратами за сегодня
  - при выборе проекта → КОМАНДА "Открыть проект"

КОМАНДА: "Открыть проект"
ДАННЫЕ:
  - kv.get("project:{projectId}")
  - kv.get("billing:{projectId}")
  - kv.get("alerts:{projectId}")
  - kv.get("autoreports:{projectId}")
  - R2.get("meta/campaigns/{projectId}.json")
  - R2.get("project-leads/{projectId}/list.json")
СОБЫТИЕ:
  - рассчитать CPA, лиды сегодня/всего
  - отправить карточку проекта с текстом из раздела 2.4

КОМАНДА: "Оплата"
ДАННЫЕ:
  - kv.get("billing:{projectId}")
  - R2.get("payments/{projectId}/history.json")
СОБЫТИЕ:
  - показать экран оплаты
  - обработать нажатия кнопок (+30 дней, 350$/500$, ввод даты)
  - при вводе даты парсить любое сообщение и сохранять next_payment_date
  - после изменения обновить billing и history, отправить подтверждение
```

Codex/разработчик должен дополнить таблицу аналогично для всех команд: "Лиды", "Отчёт по рекламе", "Экспорт", "Авто-отчёты", "КPI", "Удалить" и т. д.

## 7. Master Prompt для Codex

Ниже полностью готовый промпт для Codex/ИИ. Скопируйте блок целиком:

```
Ты — senior full-stack разработчик, который продолжает и исправляет уже существующий проект Cloudflare Worker + Telegram Bot + Web Portal для отслеживания рекламы.

Важные условия:
1. В репозитории уже есть .env/секреты. НЕЛЬЗЯ добавлять новые переменные или менять формат существующих. Используй: BOT_TOKEN, TELEGRAM_BOT_TOKEN, TELEGRAM_SECRET, WORKER_URL, ADMIN_IDS, ADMIN_ID, FB_APP_ID, FB_APP_SECRET, FB_LONG_TOKEN, CLOUD_FLARE_API_TOKEN, CLOUD_FLARE_AC_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_ACCOUNT_ID, R2_ENDPOINT, DEFAULT_TZ.
2. Все данные хранятся в Cloudflare KV и R2. Обязательно соблюдай архитектуру ключей:
  - KV: facebook-auth:{userId} (совместимо с fb_auth:{userId}), projects_by_user:{userId}, project:{projectId}, billing:{projectId}, alerts:{projectId}, autoreports:{projectId}.
   - R2: project-leads/{projectId}/list.json, project-leads/{projectId}/{leadId}.json, meta/campaigns/{projectId}.json, payments/{projectId}/history.json.
3. Telegram-бот, портал и Meta-интеграция уже существуют, но логика частично сломана. Нужно довести функционал до рабочего состояния (ботовые меню, портал, оплаты, KPI, Meta webhook, алерты, автоотчёты).

Задачи:
1. Реализовать/проверить модели данных и CRUD строго по структурам из ТЗ.
2. Реализовать ВСЮ логику Telegram-бота:
   - Главное меню, авторизация Facebook, список проектов, карточка проекта, все кнопки и сценарии (Изменить данные, Чат-группа, Лиды, Отчёт по рекламе, Экспорт, Портал, Оплата, Авто-отчёты, KPI, Удалить и т. д.).
   - Команды ввода дат должны парсить любое сообщение в ожидаемом формате, без ответов «Команда X не поддерживается».
   - Карточка проекта обязана показывать «Лиды: X (сегодня) | Y (всего)» и корректный CPA.
3. Реализовать алерты: лиды в очереди, кампании на паузе >24ч, напоминания об оплате. Форматы сообщений — как в ТЗ.
4. Исправить портал: прелоадер, параллельные запросы к API, отображение KPI, лидов, кампаний, оплат.
5. Разбить работу на логические итерации. После каждой итерации обновлять README (что сделано, что дальше, как тестировать) и обязательно включать инструкцию «Как подключить Telegram webhook» с URL `https://${WORKER_URL}/tg-webhook?secret=${TELEGRAM_SECRET}` и примером `curl` для `setWebhook`.
6. Не ломать текущую структуру проекта — только дорабатывать и доводить до рабочего состояния.

Начни с анализа репозитория, опиши план итераций в README, затем реализуй всё по шагам, опираясь на это ТЗ.
```

---

Документ можно использовать как рабочее ТЗ для команды и как master prompt для Codex.
