# Операционный runbook Targetbot

Документ помогает дежурной команде быстро реагировать на сбои и восстанавливать работу воркера. Все описанные шаги
рассчитаны на окружение Cloudflare Workers + Telegram + Meta (Facebook).

## 1. Как обнаружить проблему

| Симптом | Где проверить | Что означает |
| --- | --- | --- |
| Бот молчит или отвечает с задержкой | `/health?ping=telegram`, `getWebhookInfo` в панели Cloudflare | Вебхук отключён, превышен лимит Telegram или воркер не отвечает |
| Админ-панель показывает `🔴 Нет данных` | `/manage/meta?refresh=1` | Ошибка Meta OAuth, токен истёк, Graph API недоступен |
| Ошибка 5xx/timeout на всех маршрутах | Cloudflare Dashboard → Workers → Logs, статус [status.cloudflare.com](https://status.cloudflare.com/) | Проблема у Cloudflare или баг в релизе |
| Отчёты не приходят по расписанию | KV ключ `schedule:<projectId>` и логи `cron` | Планировщик упал, расписания повреждены, нет связи с Meta |
| Клиентский портал выдаёт 403 | `/p/<code>?sig=...` + `getProjectPortalSignature` | Неверная подпись, токен удалён, запрос без `sig` |

## 2. Приоритеты устранения

1. **Связь с Telegram** — без вебхука админы не получат алерты и отчёты.
2. **Актуальный токен Meta** — все данные панели и алертов зависят от Graph API.
3. **Расписания отчётов** — сохраняем отправку в клиентские чаты.
4. **Клиентский портал** — должен оставаться доступным хотя бы в режиме кеша.
5. **Доп. сервисы (интеграционные скрипты, выгрузки)** — восстанавливаем после критичных функций.

## 3. Telegram: диагностика и восстановление

1. `npm run check:config -- --ping-telegram` — проверяет токен и доступность Telegram API.
2. `/manage/telegram/webhook?token=<...>&action=info` — смотрим текущий вебхук, если `pending_update_count > 0`, используем `drop=1`.
3. Чтобы очистить и поднять webhook заново:
   - `...&action=delete&drop=1`
   - `...&action=refresh&drop=1` (без `url` будет использован рекомендованный путь `/telegram/<shortToken>`)
4. Если Telegram вернул ошибку 409/429 — уменьшаем частоту запросов, проверяем отсутствие параллельных воркеров.
5. При ошибке `TOKEN_INVALID` — обновляем секрет `BOT_TOKEN` и перезапускаем деплой (`npm run sync:secrets`, `npm run deploy`).

## 4. Meta OAuth и Graph API

1. Проверяем `/fb_debug?token=<SESSION>` — убедиться, что long-lived токен присутствует.
2. `npm run test:integration -- --meta-token <META_MANAGE_TOKEN>` — прогоняет запрос `/manage/meta`.
3. Если токен протух:
   - Открываем кнопку «Авторизоваться в Facebook» из админки и проходим OAuth заново.
   - После редиректа воркер сохранит токен в KV (`meta:long_token`).
4. При ошибках `(#100)`, `(#200)` — сверяем список полей в `src/meta/` (не трогать поля, требующие дополнительных прав).
5. Если Graph API недоступен глобально — переключаем админку в режим кеша (ответ `/manage/meta` выдаёт `cached: true`).

## 5. Расписания и автопауза

1. Проверяем KV ключи `schedule:<projectId>` и `kpi:<projectId>` через Cloudflare Dashboard → KV Explorer.
2. Если расписание пустое — запускаем сессию `/admin → Проекты → Настройки → Расписание` и подтверждаем новое время.
3. Для экстренной остановки отчётов ставим флаг `mute=true` в KV или удаляем расписание (бот сообщит в админку).
4. Автопауза кампаний включается из карточки проекта; при подозрении на неправильную остановку проверяем `project:<id>:autopause`.

## 6. Клиентский портал

1. Проверяем ссылку из кнопки «🌐 Портал» — она содержит сигнатуру `sha256(code:token)`.
2. При 403/401 — проверяем существование проекта (`project:<code>`), наличие `portal.token` или глобального `PORTAL_TOKEN`.
3. Если данные устарели — добавляем `?refresh=1` или вызываем `/manage/meta?refresh=1`.
4. Форма «Написать менеджеру» использует webhook `GS_WEBHOOK` или сообщения в Telegram; при отсутствии ключей запрос логируется в KV.

## 7. Релиз и откат

1. **Проверка перед деплоем**
   - `npm run check:config`
   - `npm run test:integration -- --base https://<worker>` (по возможности)
2. **Деплой** — `npm run deploy` или GitHub Actions `Deploy` workflow.
3. **Откат**
   - Через Cloudflare Dashboard → Workers → Versions → выбрать предыдущую версию → Promote.
   - Либо запустить workflow с параметром `ref=<tag/commit>`.
4. **Post-deploy** — проверить `/health`, `/admin`, `/p/<code>`, а также получить отчёт «Сегодня».

## 8. Работа с секретами

1. Локально: `npm run sync:secrets -- --env production` (перед этим экспортировать переменные).
2. GitHub Actions: обновить Secrets в разделе Settings → Secrets and variables → Actions.
3. Cloudflare Dashboard: Workers → Settings → Variables → редактировать секрет.
4. После обновления `FB_APP_SECRET`/`FB_APP_ID` перезапустить OAuth, после `BOT_TOKEN` — `deleteWebhook` + `setWebhook`.

## 9. Таблица контактов и эскалации

| Ситуация | Кого звать | Канал |
| --- | --- | --- |
| Отказ Cloudflare | [support.cloudflare.com](https://support.cloudflare.com/hc/en-us/requests/new) | Портал поддержки |
| Ошибки Telegram (429/5xx) | @BotSupport | В Telegram |
| Проблемы Meta | [developers.facebook.com/support](https://developers.facebook.com/support) | Support Case |
| Секреты/CI | DevOps / владелец репозитория | Slack / Telegram |

## 10. Журнал и постмортем

1. Заполняем краткую запись в `docs/incidents/<YYYY-MM-DD>-<slug>.md`: описание, действия, выводы.
2. Обновляем README (раздел «Что предстоит сделать дальше»), если обнаружено новое требование.
3. Планируем улучшения (например, дополнительный алерт) и заводим задачу в трекере.

---

Runbook обновляется по мере появления новых модулей и регламентов. Следите за актуальностью инструкций после каждого релиза.
